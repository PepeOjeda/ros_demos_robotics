# Laser demo

Minimal demo for Hokuyo 2D Lidar. Use 
```
rosdep install --from-paths src --ignore-src -r -y
```

to install all dependencies. Run demo with
```
ros2 launch laser_demo laser_demo_launch.py 
```
